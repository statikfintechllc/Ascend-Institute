name: gremlin-scraper
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - playwright
  - beautifulsoup4
  - lxml
  - aiohttp
  - requests
  - tqdm
  - rich
  - loguru
  - flask  # Required for API interop with dashboard
  - flask-socketio  # Real-time messaging
  - pip:
      - undetected-chromedriver
      - websockets
